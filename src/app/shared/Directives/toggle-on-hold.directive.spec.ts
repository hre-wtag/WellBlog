import { ToggleOnHoldDirective } from './toggle-on-hold.directive';

describe('ToggleOnHoldDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleOnHoldDirective();
    expect(directive).toBeTruthy();
  });
});
